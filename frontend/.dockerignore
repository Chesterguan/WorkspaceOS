# Dependencies — reinstalled inside the image
node_modules/

# Next.js build output — rebuilt inside the image
.next/
out/

# Local environment files (secrets are passed via docker-compose environment block)
.env
.env.*
!.env.local.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Misc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
README.md
